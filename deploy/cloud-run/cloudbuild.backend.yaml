substitutions:
  _IMAGE: ""

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - deploy/cloud-run/backend.Dockerfile
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
